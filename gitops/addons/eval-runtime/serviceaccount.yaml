apiVersion: v1
kind: ServiceAccount
metadata:
  name: eval-runner
  namespace: eval-runner
  annotations:
    # IRSA — patched by ArgoCD ApplicationSet elements per-env to the
    # eval_runner_role_arn output of terraform/components/eval-runtime.
    eks.amazonaws.com/role-arn: REPLACE_BY_APPLICATIONSET
