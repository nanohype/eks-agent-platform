# Test-only dependency. boto3 is provided by the Lambda runtime in production,
# so it is not a pinned application dependency; the unit tests only need it
# importable (the AWS clients are mocked).
boto3
