# expect: a DynamoDB table has no default partition key
# CRD CEL: kind=keyValue requires the keyValue block, whose partitionKey is required.
platform:
  name: cover
  tenant: coverco
datastores:
  - name: sessions
    kind: keyValue
